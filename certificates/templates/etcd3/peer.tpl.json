{
    "CN": "*.master.k8s-${project}-${environment}.internal",
    "hosts": [
        "*.master.k8s-${project}-${environment}.internal",
        "127.0.0.1"
    ],
    "key": {
        "algo": "ecdsa",
        "size": 256
    },
    "names": [
      {
          "C": "${country}",
          "L": "${city}",
          "O": "${organization}",
          "ST": "${state}"
      }
    ]
}
